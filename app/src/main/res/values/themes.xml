<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BookApp1" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/black</item>
        <item name="colorControlActivated">@color/yellow</item>
    </style>
    <style name="Theme.BookApp1" parent="Base.Theme.BookApp1" />
    <!-- res/values/styles.xml -->
        <style name="CustomSearchView" parent="Widget.AppCompat.SearchView">
            <!-- Set the color of the query text to white -->
            <item name="android:textColor">#FFFFFF</item>
            <!-- Set the hint text color to white -->
            <item name="android:textColorHint">#FFFFFF</item>
            <!-- Set the background of the query text field (optional) -->
            <item name="android:queryBackground">@android:color/white</item>
            <!-- Set the search icon to a custom white icon -->
            <item name="android:searchIcon">@drawable/ic_search</item>
        </style>

    <!-- res/values/styles.xml -->
</resources>